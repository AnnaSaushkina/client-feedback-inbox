1. Архитектурные принципы фронт приложений

2. Архитектурный стиль
   комопнентная архитектура с централизированным стором
   taskStore → хранит данные
   Button → вызывает действие
   TaskCard → отображает данные

3. однонаправленный поток данных
   Store → UI → Action → Store → UI
