# URL бэкенда. Без значения — локальный localStorage-режим.
VITE_API_URL=https://your-api-url/api

# Строка подключения к PostgreSQL (только для сервера)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskdb
