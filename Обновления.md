
**Обновление фронта:**
```bash
# Локально
pnpm build
scp -r dist/* root@157.22.231.139:/var/www/html/
```

**Обновление бэка:**
```bash
# апдейт в GitHub
git add .
git commit -m "update"
git push

# На сервере
ssh root@IP
cd ~/task-manager-server
git pull
pnpm build
pm2 restart task-manager
```
